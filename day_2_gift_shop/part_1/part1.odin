package part1

import "../../utils"
import "core:log"

run :: proc(aoc: utils.aoc_data) -> u64 {
	log.debug("you most certainly can't see this one from the TEST!??!!?!?")
	log.info("can you see this from the TEST!!??!!??!?!?")
	return 0
}
